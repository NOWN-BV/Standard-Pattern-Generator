// prototypes/veil-standard-pattern/main.jsx
// Standalone entry point for the prototype. See README.md.

import React from 'react';
import { createRoot } from 'react-dom/client';
import VeilStandardPatternGenerator from './VeilStandardPatternGenerator.jsx';

createRoot(document.getElementById('root')).render(<VeilStandardPatternGenerator />);
